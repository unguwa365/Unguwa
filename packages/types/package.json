{
  "name": "@unguwa/types",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "lint": "eslint . --ext .ts,.js",
    "format": "prettier --check ."
  },
  "devDependencies": {
    "@unguwa/eslint-config-custom": "workspace:*"
  }
}
